{
  "name": "bleed",
  "version": "1.0.0",
  "description": "My personal design system.",
  "author": "Dominic Neo",
  "type": "module",
  "main": "dist/bleed.css",
  "files": [
    "dist",
    "src",
    "tokens"
  ],
  "scripts": {
    "build:tokens": "node build.js tokens",
    "build": "node build.js",
    "watch": "node build.js && node --watch-path=./src --watch-path=./tokens build.js",
    "test": "node build.js && node build.js --check"
  },
  "devDependencies": {
    "style-dictionary": "^5.5.0"
  }
}
